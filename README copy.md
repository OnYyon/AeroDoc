# AeroDoc Assistant

Интеллектуальный ассистент для поддержки технических специалистов и инженеров в аэрокосмической отрасли с использованием технологии Large Language Models (LLM).

## Функционал

- 🔐 Регистрация и авторизация с выбором роли
- 📄 Загрузка документов (PDF, DOCX, DOC, TXT, JSON, XML)
- 💬 Чат-интерфейс для общения с ассистентом
- 📚 История чатов для каждого пользователя
- 🔒 Контроль доступа к документам на основе ролей
- 🤖 Интеграция с ML-сервисом для обработки запросов

## Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **SQLite** - база данных (можно легко перейти на PostgreSQL)
- **JWT** - аутентификация пользователей
- **bcrypt** - хеширование паролей

### Frontend
- **React** - библиотека для создания пользовательского интерфейса
- **Vite** - сборщик и dev-сервер
- **React Router** - маршрутизация
- **Axios** - HTTP клиент
- **Lucide React** - иконки

## Установка и запуск

### Требования

- Python 3.8+
- Node.js 16+
- npm или yarn

### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
- Windows:
```bash
venv\Scripts\activate
```
- Linux/Mac:
```bash
source venv/bin/activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

5. Создайте файл `.env` в директории `backend` (опционально, можно использовать значения по умолчанию):
```env
DATABASE_URL=sqlite:///./aerodoc.db
SECRET_KEY=your-secret-key-change-in-production
ML_SERVICE_URL=http://localhost:8001/api/ml/process
DOCUMENTS_DIR=./documents
```

6. Запустите сервер:
```bash
python main.py
```

Backend будет доступен по адресу: `http://localhost:8000`

### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev-сервер:
```bash
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:3000`

## Структура проекта

```
.
├── backend/
│   ├── main.py              # Главный файл FastAPI приложения
│   ├── database.py          # Настройка базы данных
│   ├── models.py            # SQLAlchemy модели
│   ├── schemas.py           # Pydantic схемы
│   ├── auth.py              # Аутентификация и авторизация
│   ├── config.py            # Конфигурация
│   └── requirements.txt     # Python зависимости
├── frontend/
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── services/        # API сервисы
│   │   └── App.jsx          # Главный компонент
│   ├── package.json
│   └── vite.config.js
├── documents/               # Директория для загруженных документов
└── README.md
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получить информацию о текущем пользователе

### Документы
- `POST /api/documents/upload` - Загрузить документ
- `GET /api/documents` - Получить список доступных документов

### Чаты
- `POST /api/chats` - Создать новый чат
- `GET /api/chats` - Получить список чатов пользователя
- `GET /api/chats/{chat_id}/messages` - Получить сообщения чата
- `POST /api/chats/{chat_id}/messages` - Отправить сообщение
- `DELETE /api/chats/{chat_id}` - Удалить чат

## Роли пользователей

- **engineer** - Инженер
- **technician** - Техник
- **quality_control** - Контроль качества
- **project_manager** - Менеджер проектов
- **maintenance** - Обслуживающий персонал
- **admin** - Администратор

## Интеграция с ML-сервисом

Backend отправляет запросы в ML-сервис по адресу, указанному в `ML_SERVICE_URL`. 

Формат запроса:
```json
{
  "question": "текст вопроса пользователя",
  "document_paths": ["путь/к/документу1.pdf", "путь/к/документу2.pdf"],
  "user_role": "engineer"
}
```

Ожидаемый ответ:
```json
{
  "answer": "ответ от ML-сервиса"
}
```

## Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- Контроль доступа к документам на основе ролей
- CORS настройки для защиты от межсайтовых запросов

## Разработка

### Добавление новых ролей

Роли определяются в `backend/models.py` в классе `UserRole`. Для добавления новой роли:

1. Добавьте значение в enum `UserRole`
2. Обновите список ролей в `frontend/src/components/Register.jsx`

### Расширение функционала

Проект спроектирован с учетом расширяемости:
- Модульная структура backend
- Компонентная архитектура frontend
- Легко добавить новые endpoints и компоненты

## Лицензия

MIT

